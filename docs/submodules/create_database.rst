pyTweetBot.create_database
==========================

This file contains a function to create the database
structure and tables.

Example:
    Here is a simple example to create the database::

        >>> config = BotConfig.load("config.json")
        >>> create_database(config)

pyTweetBot.create\_database module
----------------------------------

.. automodule:: pyTweetBot.create_database
    :members:
    :undoc-members:
    :show-inheritance:
